-- -----------------------------------------------------
-- Stacks: Track items (consumables, currencies, etc.)
-- -----------------------------------------------------

local L, C, G = unpack(select(2, ...))
